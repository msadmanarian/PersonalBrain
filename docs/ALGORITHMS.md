# PersonalBrain Algorithms Specification

## 1. SuperMemo-2 (SM-2) Spaced Repetition
$$EF' = EF + (0.1 - (5 - q) \times (0.08 + (5 - q) \times 0.02))$$

## 2. PageRank Centrality on Knowledge Graphs
$$PR(u) = \frac{1 - d}{N} + d \sum_{v \in B_u} \frac{PR(v)}{L(v)}$$

## 3. Critical Path Method (CPM)
Identifies the longest dependent execution sequence in a Task DAG to compute project duration bounds.
